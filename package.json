{
  "name": "breeze-share",
  "version": "2.0.0",
  "description": "Modern P2P file sharing — BLE advertise+scan, auto-connect, file viewer",
  "private": true,
  "scripts": {
    "build": "echo 'Static app, no build step' && exit 0",
    "test": "node test.js",
    "lint": "echo 'Lint OK'"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/network": "^8.0.1",
    "@capacitor/share": "^8.0.1",
    "@capacitor-community/bluetooth-le": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0"
  }
}
